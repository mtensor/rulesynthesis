#imports



#


hit, solution, stats = batched_test_with_sampling(sample, model, max_len=1 if 'RB' in args.type or 'Word' in args.type else 15, 
                                    examples=examples,
                                    query_examples=query_examples,
                                    timeout=args.timeout,
                                    verbose=True, 
                                    min_len=0, 
                                    batch_size=args.batchsize,
                                    nosearch=args.nosearch,
                                    partial_credit=args.partial_credit,
                                    seperate_query=args.seperate_query,
                                    max_rule_size= 100 if 'RB' in args.type or 'Word' in args.type else 15)




def test_deepcoder(sample, dc_model, 
                   enum_only=False,
                   timeout=args.timeout,
                   **kwargs
                   mdlIncrement):

    

    while incriment thing
    
    for ll,_,p, usedPrims in g.enumeration(Context.EMPTY,[],request,
                               50., lowerBound=49.5, maximumDepth=20 ,uniquePrims=True):
        #print(p)
        #print(usedPrims)
        #print()
        while timeout has not finished:
        i+=1
        if usedPrims != oldUsedPrims:
            print(p)
            print(usedPrims)
            oldUsedPrims = usedPrims
            print(i)





    return hit, solution, stat